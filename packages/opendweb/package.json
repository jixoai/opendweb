{
  "name": "opendweb",
  "version": "0.4.2",
  "description": "opendweb — application-level fabric networking: self-hosted server CLI (relay + rendezvous)",
  "license": "MIT OR Apache-2.0",
  "bin": {
    "opendweb": "./bin/opendweb.mjs"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "@jixo/opendweb-server-binary": "workspace:*",
    "smol-toml": "^1.8.0",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@types/node": "22",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  },
  "scripts": {
    "checkjs": "tsc --allowJs --checkJs --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --esModuleInterop --skipLibCheck --types node src/*.mjs",
    "test": "npm run checkjs && node --test --test-force-exit --test-concurrency=1 test/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jixoai/opendweb.git"
  }
}